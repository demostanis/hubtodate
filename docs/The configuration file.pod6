=begin pod

=head1 The configuration file

A file located at C</etc/hubtodate.conf>,
used to specify many settings used by HubToDate.
However, most of them have default values, which
means you shouldn't have to edit it, except if
you really know what you are doing.

=end pod
